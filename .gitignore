*.out
*.log
node_modules/

# Run logs are tracked, logs/ included. summarise-runs.py is tested against
# real ones, and the v1.7 and v1.8a files cover the 24- and 28-column paths.
# logs/ is where the simulator writes overnight, so expect it to grow: a
# night of marathon mode is one file per colony lifetime.
